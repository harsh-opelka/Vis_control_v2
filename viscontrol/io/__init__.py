"""I/O subsystems: camera, OPC UA server, file storage, web sidecar.

Note: this subpackage shadows the stdlib ``io`` module only inside ``viscontrol``;
absolute imports of stdlib ``io`` still resolve correctly.
"""
