AUTOSTART — IMPLEMENTATION RESULT
===================================

FILES CHANGED
-------------
1. viscontrol/core/config.py
   - _AppSection gained: auto_start_detection: bool = False
   - Wired automatically: AppConfig.app is a _AppSection (Field(default_factory=_AppSection)),
     so cfg.app.auto_start_detection is available with no other code changes.

2. config/default.yaml
   - Added `auto_start_detection: false` under `app:` (default OFF, matches
     the pydantic default — belt and suspenders).

3. viscontrol/main.py
   - After `window.show()` and the startup-banner handling (i.e. after
     MainWindow.__init__ has fully built the window/children/camera-start,
     and after the production-mode PLC-client/web-server setup blocks have
     run), added:

       logger.info("[AUTOSTART] auto_start_detection={}", cfg.app.auto_start_detection)
       if cfg.app.auto_start_detection:
           logger.info("[AUTOSTART] auto_start_detection enabled, starting detection")
           logger.info("[AUTOSTART] triggering _enable_detection at t={}ms after window shown", 0)
           window._enable_detection()

   - No QTimer.singleShot delay was added. Per STEP 1/part C findings
     (see autostart_inspection.txt): _enable_detection() has zero
     preconditions (it is a single-line flag flip, `self._detection_enabled
     = True`), and there is no camera/PLC "readiness" signal anywhere in the
     codebase to hook into — the closest state, `_camera_running`, is
     already set to True synchronously inside MainWindow.__init__ (before
     window.show() even runs), and it does not represent actual backend
     connection completion regardless. Adding a delay here would be
     guessing at a wait condition the code doesn't have, which the task
     explicitly said not to do. This is a direct, unconditional call.

4. config/local.yaml (WINDOWS dev machine) — NOT modified.
   Per the task's explicit instruction, auto_start_detection: true belongs
   only in the Jetson's config/local.yaml, not the Windows dev one. Left
   untouched.

NOT DONE — REQUIRES ACTION ON THE JETSON ITSELF
------------------------------------------------
config/local.yaml on the Jetson (/home/jetson/Vis_control_v2/config/local.yaml)
could NOT be edited from here: this session is running on the Windows dev
machine (win32), with no filesystem or shell access to the Jetson. That file
is per-machine (config/local.yaml is the local-override file, distinct per
deployment) and is not something this Windows session can reach.

Action needed on the Jetson, once these code changes are deployed there, add
under the existing `app:` key in its config/local.yaml:

    app:
      auto_start_detection: true

(merge into the existing `app:` section if one is already present in that
file — do not duplicate the key).

VERIFICATION
------------
- `python -m py_compile` on config.py, main.py, main_window.py: all pass,
  no syntax errors.
- Config precedence confirmed by reading load_config()/_deep_merge(): default.yaml
  is loaded first, local.yaml deep-merged on top only for keys present in
  local.yaml, and the pydantic field default (False) applies wherever a key
  is absent from both files. Consequence: on the Windows dev machine, whose
  local.yaml does NOT define app.auto_start_detection, cfg.app.auto_start_detection
  evaluates to False (from config/default.yaml, itself matching the pydantic
  default) — the added `if cfg.app.auto_start_detection:` block in main.py
  is skipped entirely, so window._enable_detection() is never called at
  startup. Detection remains OFF until the operator presses Start, exactly
  as before this change.
- Could not execute the app or its test suite live in this session — no
  Python environment on this machine has the project's dependencies
  (PySide6, pydantic, pyyaml) installed (checked the available local venvs;
  none had `yaml`/`pydantic` importable). Verification above is via static
  code reading of config.py's merge/precedence logic plus syntax compilation,
  not a live run. Recommend a manual smoke test on both machines before
  relying on this in production:
    - Windows: launch app normally, confirm Start button still required.
    - Jetson (after adding the local.yaml key above): launch app via
      systemd, confirm log lines
        [AUTOSTART] auto_start_detection=True
        [AUTOSTART] auto_start_detection enabled, starting detection
        [AUTOSTART] triggering _enable_detection at t=0ms after window shown
      appear and detection begins without any button press.

CONSTRAINTS RESPECTED
----------------------
- _enable_detection() itself: NOT modified.
- config/default.yaml: auto_start_detection defaults to false (only true in
  the Jetson's local.yaml, which is outside this session's reach — see above).
- Start button: unchanged, still wired (iv.start_clicked.connect(self._enable_detection)
  at main_window.py:509) and still works if pressed again (idempotent: it
  just re-sets self._detection_enabled = True; pressing it again after
  auto-start is a harmless no-op state-wise).
