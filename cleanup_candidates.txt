CLEANUP CANDIDATES — READ-ONLY REPORT
=====================================================================
Generated by inspection only. Nothing was deleted, moved, or modified.
Project root: C:\Users\harshkumar.rai\PyCharmMiscProject\Vis_control_v2

Legend: SAFE TO DELETE / KEEP / UNCERTAIN (per item, as instructed).


SECTION 1 — TXT REPORTS (root-level, one-off Claude Code run outputs)
=====================================================================
Found 20 root-level .txt files matching this pattern (more than the ~16
estimated in the prompt). All were checked with:
  grep -rl "<filename>" --include="*.py" --include="*.md" --include="*.yaml"
    --include="*.yml" --include="*.cfg" --include="*.ini" --include="*.toml" .
(tests/*.py is a subset of *.py, so this also covers "referenced in any
test file").

Two files are large raw-source dumps rather than narrative reports
(layer3_main_window.txt, layer3_row_grouping.txt) — noted below.

--------------------------------------------------------------------
architecture_report.txt          29,702 bytes   modified Jul 31 11:16
  First lines: "VISCONTROL — ARCHITECTURE REPORT" /
               "Generated by reading the repository only (no code changes made)."
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere. (Note: this one reads
  as general living documentation rather than a fix-specific report; the
  person may want to keep it for onboarding purposes even though nothing
  requires it to exist.)

autostart_inspection.txt          5,078 bytes   modified Aug 21 10:21
  First lines: "AUTOSTART INSPECTION" / "===================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

autostart_result.txt              5,101 bytes   modified Aug 21 10:24
  First lines: "AUTOSTART — IMPLEMENTATION RESULT" / "==================================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

calibration_bugfix_inspection.txt 4,403 bytes   modified Aug 20 11:35
  First lines: "SIZE CALIBRATION BUGFIX — STEP 1 INSPECTION REPORT" / "====...===="
  References found: viscontrol/core/size_calibration.py:149 (comment: "...see
  calibration_bugfix_inspection.txt part B): a fixed value tied to...").
  VERDICT: SAFE TO DELETE (functionally) — the only "reference" is a
  human-readable comment pointer in a docstring, not an import or file
  read. Deleting the .txt leaves that comment mentioning a file that no
  longer exists (harmless to the program, but a dangling doc pointer for
  a future reader). Flagging so the person can decide whether to also
  trim the comment.

calibration_bugfix_result.txt    11,645 bytes   modified Aug 20 11:40
  First lines: "SIZE CALIBRATION BUGFIX — RESULT REPORT" / "=========================================="
  References found: tests/test_size_calibration.py:12 (comment: "...module
  entirely — see calibration_bugfix_result.txt. Cluster tolerance is a...")
  and viscontrol/ui/main_window.py:2362 (comment: "...no longer guesses at
  either (see calibration_bugfix_result.txt).").
  VERDICT: SAFE TO DELETE (functionally) — same caveat as above: comment-
  only pointers in a test docstring and a main_window.py comment, not
  imported/executed/read by any code.

calibration_inspection.txt       18,752 bytes   modified Aug 20 10:46
  First lines: "SIZE CALIBRATION — STEP 0 INSPECTION REPORT" / "============================================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

calibration_result.txt           11,431 bytes   modified Aug 20 11:01
  First lines: "SIZE CALIBRATION — STEP 6 VERIFY / REPORT" / "============================================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

cycle_fix_result.txt             10,089 bytes   modified Aug 19 12:04
  First lines: "CYCLE LIFECYCLE FIX — RESULT" / "=============================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

cycle_inspection.txt             12,545 bytes   modified Aug 19 11:59
  First lines: "CYCLE LIFECYCLE INSPECTION" / "==========================="
  References found: viscontrol/ui/main_window.py:804 (comment: "...
  cycle_inspection.txt). Session/cycle boundaries are now owned...").
  VERDICT: SAFE TO DELETE (functionally) — comment-only pointer, same
  caveat as above.

image_collection_inspection.txt   9,724 bytes   modified Aug 26 09:04
  First lines: "STEP 0 — INSPECTION REPORT (training-data image collection feature)" / "===...==="
  References found: none. (This and the next file are the two reports
  produced by the immediately preceding session in this same conversation.)
  VERDICT: SAFE TO DELETE — not referenced anywhere.

image_collection_result.txt       9,533 bytes   modified Aug 26 09:10
  First lines: "STEP 7 — RESULT REPORT (training-data image collection feature)" / "===...==="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

layer3_main_window.txt          376,412 bytes   modified Jul 7 15:05
  First lines (file appears to be a raw source dump, not a narrative
  report — content looks like a full past copy of main_window.py, saved
  with unusual byte spacing/encoding rather than plain UTF-8 text):
  "MainWindow — wires the sidebar, views, and runtime services together."
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere. Distinct in kind from
  the other reports: this looks like an old full-file snapshot (possibly
  from a "Layer 3" experiment/checkpoint) rather than an inspection/result
  report; flagging in case it has archival value the person wants to keep
  under a different name instead of deleting outright.

layer3_row_grouping.txt          49,118 bytes   modified Jul 7 15:05
  First lines: same pattern as above — raw dump, appears to be an old
  snapshot of row_grouping.py content: "Row grouping for the cloth side
  (behind the USE_ROW_GROUPING toggle)."
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere. Same archival caveat
  as layer3_main_window.txt above.

layout_lock_inspection.txt       15,540 bytes   modified Aug 10 09:18
  First lines: "LAYOUT-QUALITY-LOCK — STEP 0 INSPECTION REPORT" / "================================================================"
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

layout_lock_result.txt            8,957 bytes   modified Aug 10 09:47
  First lines: "LAYOUT-QUALITY-LOCK — STEP 7 VERIFY + RESULT REPORT" / "================================================================"
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

piece_row_inventory.txt          15,639 bytes   modified Aug 3 08:48
  First lines: "STEP 0 — INSPECTION REPORT" / "=========================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

piece_row_result.txt             11,924 bytes   modified Aug 3 09:38
  First lines: "STEP 10 — VERIFY AND REPORT" / "==========================="
  References found: none.
  VERDICT: SAFE TO DELETE — not referenced anywhere.

refactor_inventory.txt           17,661 bytes   modified Jul 31 13:33
  First lines: "REFACTOR INVENTORY — STEP 0" / "Generated before any code
  changes. Project root: C:\Users\harshkumar.rai\PyCharmMiscProject\Vis_control_v2"
  References found: viscontrol/core/row_associator.py:10 (comment: "...
  group-merge sweep sorted farthest-upstream-first) — see
  refactor_inventory.txt.").
  VERDICT: SAFE TO DELETE (functionally) — comment-only pointer. NOTE:
  this file's own content is also useful corroborating evidence for
  SECTION 4 below (it documents, at the time of the refactor, that
  ClusterTracker/DonePieceTracker in proximity_clustering.py were being
  deliberately kept-but-marked-DEPRECATED rather than deleted).

refactor_result.txt              17,172 bytes   modified Jul 31 14:10
  First lines: "REFACTOR RESULT — structural refactor of the transfer/stop
  control logic" / "Project root: C:\Users\harshkumar.rai\PyCharmMiscProject\Vis_control_v2"
  References found: viscontrol/core/transfer_orchestrator.py:70 (comment:
  "...fall back to (see refactor_result.txt)."); viscontrol/ui/main_window.py:1556
  (comment: "...fall back to (see refactor_result.txt).").
  VERDICT: SAFE TO DELETE (functionally) — comment-only pointers in two
  files.

window_resize_inspection.txt     17,080 bytes   modified Aug 10 10:55
  First lines: "WINDOW RESIZE/GROW-ON-STARTUP — INSPECTION REPORT (report
  only, nothing fixed)" / "================================================================"
  References found: viscontrol/ui/main_window.py:422 (comment: "...than the
  screen (See window_resize_inspection.txt).").
  VERDICT: SAFE TO DELETE (functionally) — comment-only pointer.

--------------------------------------------------------------------
SECTION 1 SUMMARY: 0 of the 20 files are imported, executed, or read by
any code path. 6 of the 20 (calibration_bugfix_inspection.txt,
calibration_bugfix_result.txt, cycle_inspection.txt, refactor_inventory.txt,
refactor_result.txt, window_resize_inspection.txt) are named in developer
comments as "see <file> for more context" — deleting them is safe in the
sense that nothing breaks, but it would orphan those comment pointers.
The other 14 have zero references of any kind.


SECTION 2 — scripts/ FOLDER
=====================================================================
scripts/analyze_detections.py
  Imports: none from viscontrol/ or tests/ import this script as a module
  (it's a standalone CLI entry point, run via `py scripts/...`, not
  imported). It itself imports `viscontrol.core.detection_recorder.
  load_recording`.
  README/CI/systemd: not mentioned in README.md directly by name, but its
  companion recorder (detection_recorder.py) and its role are consistent
  with the rest of the tooling; no systemd unit or CI config exists in
  this repo at all (grepped for *.service and .github/** — none found).
  What it does: offline, read-only CLI tool that loads a
  detection_recorder.py JSONL recording and prints a plain-text
  diagnostic report (piece-match distance/gap statistics) to stdout, to
  help tune matching-gate config values — it does not replace or import
  the production PieceTracker matcher.
  VERDICT: UNCERTAIN — not referenced by any README/test, but it is a
  clearly-labeled, self-contained debugging/analysis utility (its own
  docstring explains its exact non-production purpose), not obviously
  abandoned. Whether it's still actively used depends on whether anyone
  is still generating/analyzing detection_recorder.py JSONL files, which
  isn't determinable from static inspection alone.

scripts/generate_test_images.py
  Imports: not imported by viscontrol/ or tests/ (standalone CLI script).
  README/CI/systemd: YES — README.md:75 ("py scripts/generate_test_images.py")
  documents it as a one-time setup step, and README.md:196 lists it in the
  repo-layout section ("regenerate the synthetic dough frames"). No CI/systemd
  reference (none exist in this repo).
  What it does: generates the synthetic dough images that live in
  assets/test_images/ (see SECTION 3) for MockCamera/manual demo use —
  a one-off generator you'd rerun only if you wanted to regenerate/change
  those fixture images.
  VERDICT: KEEP — actively documented and referenced by: README.md (setup
  instructions + repo layout section).

scripts/simulate_plc.py
  Imports: not imported by viscontrol/ or tests/ (standalone CLI script,
  uses `asyncua` directly).
  README/CI/systemd: YES — README.md:49-50 and README.md:140-141 document
  it as the way to stand in for a real B&R PLC over OPC UA during
  integration testing on a workstation.
  What it does: a stand-in PLC simulator — connects to the VisControl OPC
  UA server and pulses `TuchabzugRunning` True/False on a configurable
  cadence, for manual bench integration testing without real hardware.
  VERDICT: KEEP — actively documented and referenced by: README.md
  (integration-testing instructions, twice).


SECTION 3 — assets/test_images/
=====================================================================
Files: .gitkeep, 01_clean.png, 02_clean.png, 03_clean.png,
10_row_fused.png, 11_row_fused.png, 20_column_fused.png, 30_unknown.png,
40_cloth_at_line.png, 50_empty_belt.png (9 images + 1 placeholder).

Grepped every image filename (with and without extension) across the
whole repo, and specifically across tests/:
  - No test file references any of these 9 filenames directly (confirmed:
    tests/test_camera.py exercises MockCamera exclusively against
    pytest's own tmp_path fixture directories, never against
    assets/test_images/).
  - All 9 filenames DO appear in scripts/generate_test_images.py (it's
    the generator that produces them — see SECTION 2) and are the
    literal default for config.mock_camera.image_dir
    ("assets/test_images", config/default.yaml / config.py:43), which
    viscontrol/io/camera.py's MockCamera reads at runtime in demo mode
    when no real Basler camera is present.

.gitkeep
  VERDICT: KEEP — standard git convention to keep an otherwise-image-only
  directory tracked; not itself a "cleanup candidate" in the sense asked
  about, just infrastructure.

01_clean.png / 02_clean.png / 03_clean.png / 10_row_fused.png /
11_row_fused.png / 20_column_fused.png / 30_unknown.png /
40_cloth_at_line.png / 50_empty_belt.png
  VERDICT for each: FLAGGED — NOT referenced by any test in tests/.
  However, KEEP is still the right call for all 9: they are the default,
  config-driven runtime asset for MockCamera (demo mode / no camera
  attached), not orphaned files — the automated tests just don't happen
  to exercise that particular runtime path with these particular files
  (they use synthetic tmp_path fixtures instead). Deleting them would not
  break any test, but would break the documented "run in demo mode with
  no camera" experience described in README.md.


SECTION 4 — DEAD / RETIRED CODE
=====================================================================
grep -in "DEPRECATED|retired|superseded|do not use in the fire path"
across all *.py files. Two categories of hits:

(a) Genuine dead-code markers (the literal phrases the prompt named):

  viscontrol/detection/proximity_clustering.py:122-123
    "# DEPRECATED: superseded by viscontrol/core/transfer_orchestrator.py
     # Retained only for existing tests. Do not use in the fire path."
    → marks class ClusterTracker (lines 124-226).

  viscontrol/detection/proximity_clustering.py:237-238
    same two-line marker → marks class DonePieceTracker (lines 239+).

  Import check: grepped the whole repo for "ClusterTracker" and
  "DonePieceTracker". Every hit outside proximity_clustering.py itself is
  a comment/docstring mention for historical context (config.py,
  main_window.py, column_learning.py, and several of the SECTION 1 .txt
  reports) — none of them is an actual `from ... import ClusterTracker`
  or `DonePieceTracker(...)` call. Also grepped tests/ specifically for
  both names: zero matches. So despite the comment's own claim
  ("Retained only for existing tests"), NO test currently imports either
  class either — they appear to be fully dead code today, not even
  test-only. (refactor_inventory.txt, written at the time of the
  original refactor, independently corroborates this: it already noted
  "no test references it" for both classes when the DEPRECATED markers
  were added.)
  VERDICT: SAFE TO DELETE — not imported by any production code or any
  test (main_window.py only imports PieceConfirmationTracker,
  cluster_by_tangent, pieces_from_detections from this module — not
  these two classes).

  viscontrol/detection/row_grouping.py:1-14 (module docstring)
    "Row/grid grouping helpers — retired from the fire decision. ... The
     functions in this module (group_by_gap, group_rows, group_into_rows,
     reject_group_outliers, RowLineTracker) are no longer wired into any
     fire decision — they're kept as unused-but-harmless utilities rather
     than deleted outright, since no callers were confirmed gone for
     certain across the whole codebase."
  Import check: grepped for group_by_gap, group_rows, group_into_rows,
  reject_group_outliers, RowLineTracker across the whole repo. Matches
  only inside row_grouping.py itself and the old layer3_row_grouping.txt
  dump — no other .py file (production or test) references any of them.
  Note: the same module docstring says `leading_edge_x` and
  `median_piece_diameter` (different functions, same file) ARE still
  live — confirmed via main_window.py imports (line 57:
  `from viscontrol.detection.row_grouping import leading_edge_x,
  median_piece_diameter`) — so this verdict applies only to the five
  named retired functions/class, not the whole file.
  VERDICT: SAFE TO DELETE for group_by_gap / group_rows / group_into_rows
  / reject_group_outliers / RowLineTracker specifically — the module
  itself must be KEPT (leading_edge_x / median_piece_diameter are
  actively imported by main_window.py).

  viscontrol/core/row_tracker.py — "RowGroupState.RETIRED" and similar
  hits here are an active runtime enum VALUE (a row-lifecycle state a
  RowGroup transitions into), not a deprecation marker for the module
  itself — this is normal business logic, not dead code. Excluded from
  the verdicts above for that reason. See below, though: this whole
  module has a separate, stronger dead-code signal not caught by the
  literal grep.

(b) Additional dead-module signal found by cross-referencing imports
    (not caught by the literal keyword grep, but directly relevant):

  viscontrol/core/row_tracker.py and viscontrol/core/piece_tracker.py
  Neither file uses the words DEPRECATED/retired/superseded about
  itself. However: grepping the whole viscontrol/ package for
  "row_tracker" / "piece_tracker" returns ONLY the files themselves —
  no other module in viscontrol/ imports either one. The only importers
  anywhere in the repo are tests: tests/test_row_tracker.py,
  tests/test_piece_tracker.py, tests/test_replay.py, and
  tests/synthetic_rows.py. Per this same repo's test run in an earlier
  session, all of those test files currently FAIL with
  "AttributeError: 'AppConfig' object has no attribute 'row_tracker'" /
  '...piece_tracker' — i.e. viscontrol/core/config.py's AppConfig no
  longer even exposes the config sections these modules/tests depend on,
  which is strong evidence this pair of modules (and their four test
  files) are leftovers from an earlier tracking design that
  proximity_clustering.py / transfer_orchestrator.py superseded, and
  were never fully removed.
  VERDICT: UNCERTAIN — functionally these look dead (nothing in
  production imports them, and their own tests are already broken), but
  confirming "safe to delete" would require the person to decide whether
  test_row_tracker.py / test_piece_tracker.py / test_replay.py /
  synthetic_rows.py should also be deleted together with the two
  modules (deleting only the modules would turn today's test FAILURES
  into test COLLECTION ERRORS instead — not a clean improvement on its
  own). This is a call about test-suite scope, not something safely
  inferred from static inspection alone.


SECTION 5 — STALE GIT BRANCHES
=====================================================================
Current branch: image-collection-for-training
(git branch --merged main → only lists "main" itself: none of the
feature branches below have been merged into main.)
(git branch --merged image-collection-for-training → lists ALL 8 of the
branches below, i.e. every one of them.)

Verified precisely with `git merge-base <current> <branch>` compared to
each branch's tip commit, for every branch below: the merge-base EQUALS
the branch's own tip commit. That is a git *fact*, not a judgment call —
it means every commit ever made on each of these 8 branches is literally
an ancestor commit of image-collection-for-training's current history.
The "safe to delete" judgment call that remains is only: is
image-collection-for-training itself intended to become (or be merged
into) the project's real trunk. If yes, deleting these 8 local branches
loses no commits (they'd all still be reachable from
image-collection-for-training). If any of them are meant to represent an
alternate, still-relevant direction (not just an earlier checkpoint of
the same line of work), that would be a reason to keep it — nothing in
the history distinguishes "abandoned experiment" from "earlier waypoint
of the same line" for certain.

auto-start-detection
  Last commit: 8eddddc  2026-08-21  "add auto-start detection for
    headless Jetson launch"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — tip commit is identical to
  image-collection-for-training's own recent history (same commit hash
  appears in `git log` for the current branch), i.e. this branch pointer
  and the current branch agree exactly up to that point; current branch
  is a strict superset.

cluster-lineage
  Last commit: 7f7e125  2026-08-06  "Add piece and row tracking"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.
  Note: this is very likely the branch that introduced
  row_tracker.py/piece_tracker.py (SECTION 4b above) — its subject line
  matches those module names exactly, which is a plausible explanation
  for why those modules exist but are no longer wired into production:
  the "piece and row tracking" design from this branch was apparently
  superseded later (by proximity_clustering.py /
  transfer_orchestrator.py) without the modules/tests from this branch
  being cleaned up.

cycle-lifecycle-fix
  Last commit: e7c9d1f  2026-08-20  "cycle lifecycle fix + layout error
    code 1"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.

layout-safety-lock
  Last commit: 9986ad8  2026-08-19  "some change before lifecyle fix"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.

size-calibration
  Last commit: 0008ff9  2026-08-21  "size calibration: radius-only,
    two-pass duplicate fix"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.

tangent-cluster-no-grid
  Last commit: f7113d7  2026-07-08  "checkpoint before starting
    tangent-clustering experiment"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.
  Note: shares its exact tip commit (f7113d7) with test-933789f-baseline
  below — the two branch names currently point at the very same commit.

tangent-cluster-rebuild
  Last commit: f5c7602  2026-07-31  "orchestrator works for normal
    cases, fails for staggered ones"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — ancestor of current branch.

test-933789f-baseline
  Last commit: f7113d7  2026-07-08  "checkpoint before starting
    tangent-clustering experiment"
  Merged into main: NO.   Merged into current branch: YES (ancestor).
  VERDICT: SAFE TO DELETE (judgment call) — same tip commit as
  tangent-cluster-no-grid (see above); the name itself ("baseline")
  suggests it was a deliberately pinned reference point rather than an
  ongoing line of work, which is consistent with it being safe to retire
  now that current branch has moved well past it.

Context on `main` (excluded from per-branch analysis per the prompt, but
relevant to interpreting the above): main is NOT an ancestor of, nor
ancestor to, image-collection-for-training in a simple way — it has 2
commits of its own not present in the current branch
("wip: layer 3 + guards + speed-prediction, pre-revert checkpoint" and
"Layer 2, works 2/3 times", both older, look like abandoned early-layer
checkpoints), while image-collection-for-training has 13 commits not on
main. In other words, main itself is currently far behind this entire
line of feature-branch work — none of the 8 branches above (nor the
current branch) have ever been merged back into main. That's a separate,
larger decision (whether/when to fast-forward or merge main) outside the
scope of "stale feature branches," but worth the person's awareness.
